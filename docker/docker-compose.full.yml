# ShipSec Studio - Production Docker Compose
#
# Required Setup:
#   Run 'just prod-init' to generate required secrets in docker/.env
#
# Required Environment Variables (set in docker/.env):
#   - INTERNAL_SERVICE_TOKEN: Service-to-service auth token (auto-generated by prod-init)
#   - SECRET_STORE_MASTER_KEY: Encryption key for secrets, must be exactly 32 characters (auto-generated by prod-init)
#
# Optional Environment Variables:
#   - AUTH_PROVIDER: Authentication provider (default: clerk)
#   - CLERK_PUBLISHABLE_KEY: Clerk public key (required for Clerk auth)
#   - CLERK_SECRET_KEY: Clerk secret key (required for Clerk auth)
#   - VITE_API_URL: Frontend API URL (default: http://localhost:3211)
#   - VITE_BACKEND_URL: Frontend backend URL (default: http://localhost:3211)
#   - SHIPSEC_TAG: Docker image tag (default: latest)
#
# Usage:
#   just prod-init          # Initialize secrets (first time only)
#   just prod start-latest  # Pull and run latest release
#   just prod start         # Start with current images

services:
  # Infrastructure
  postgres:
    image: postgres:16-alpine
    container_name: shipsec-postgres
    environment:
      POSTGRES_USER: shipsec
      POSTGRES_PASSWORD: shipsec
      POSTGRES_DB: shipsec
      POSTGRES_MULTIPLE_DATABASES: temporal
    ports:
      - '5433:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db:/docker-entrypoint-initdb.d
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U shipsec']
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped

  temporal:
    image: temporalio/auto-setup:latest
    container_name: shipsec-temporal
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - DB=postgres12
      - DB_PORT=5432
      - DB_NAME=temporal
      - POSTGRES_USER=shipsec
      - POSTGRES_PWD=shipsec
      - POSTGRES_SEEDS=postgres
      - AUTO_SETUP=true
    ports:
      - '7233:7233'
    volumes:
      - temporal_data:/var/lib/temporal
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'tctl', '--address', 'localhost:7233', 'cluster', 'health']
      interval: 30s
      timeout: 10s
      retries: 5

  temporal-ui:
    image: temporalio/ui:latest
    container_name: shipsec-temporal-ui
    environment:
      - TEMPORAL_ADDRESS=temporal:7233
      - TEMPORAL_NAMESPACE=default
    ports:
      - '8081:8080'
    depends_on:
      - temporal
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://localhost:8080']
      interval: 30s
      timeout: 10s
      retries: 5

  minio:
    image: minio/minio:RELEASE.2024-10-02T17-50-41Z
    container_name: shipsec-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - '9000:9000'
      - '9001:9001'
    volumes:
      - minio_data:/data
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:9000/minio/health/live']
      interval: 30s
      timeout: 10s
      retries: 5

  loki:
    image: grafana/loki:3.2.1
    container_name: shipsec-loki
    command: -config.file=/etc/loki/local-config.yaml
    ports:
      - '3100:3100'
    volumes:
      - ./loki/loki-config.yaml:/etc/loki/local-config.yaml
      - loki_data:/loki
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://localhost:3100/ready']
      interval: 30s
      timeout: 10s
      retries: 5

  redis:
    image: redis:latest
    container_name: shipsec-redis
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 30s
      timeout: 10s
      retries: 5

  redpanda:
    image: redpandadata/redpanda:v24.2.5
    container_name: shipsec-redpanda
    command:
      - redpanda
      - start
      - --mode=dev-container
      - --smp=1
      - --reserve-memory=0M
      - --overprovisioned
      - --node-id=0
      - --check=false
      - --advertise-kafka-addr=redpanda:9092
    ports:
      - '9092:9092'
      - '9644:9644'
    volumes:
      - redpanda_data:/var/lib/redpanda/data
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:9644/v1/status/ready']
      interval: 30s
      timeout: 10s
      retries: 5

  redpanda-console:
    image: redpandadata/console:v2.7.2
    container_name: shipsec-redpanda-console
    depends_on:
      - redpanda
    environment:
      CONFIG_FILEPATH: /etc/redpanda/console-config.yaml
    ports:
      - '8082:8080'
    volumes:
      - ./redpanda-console-config.yaml:/etc/redpanda/console-config.yaml:ro
    restart: unless-stopped

  # Applications
  dind:
    image: docker:27-dind
    container_name: shipsec-dind
    privileged: true
    command: ['--host=tcp://0.0.0.0:2375', '--storage-driver=overlay2']
    environment:
      - DOCKER_TLS_CERTDIR=
    volumes:
      - docker_data:/var/lib/docker
    healthcheck:
      test: ['CMD', 'docker', 'info']
      interval: 30s
      timeout: 10s
      retries: 5
    restart: unless-stopped

  backend:
    image: ghcr.io/shipsecai/studio-backend:${SHIPSEC_TAG:-latest}
    build:
      context: ..
      dockerfile: Dockerfile
      target: backend
    container_name: shipsec-backend
    environment:
      - NODE_ENV=production
      - SHIPSEC_ENV=production
      - PORT=3211
      - DATABASE_URL=postgresql://shipsec:shipsec@postgres:5432/shipsec
      - ENABLE_INGEST_SERVICES=true
      - TEMPORAL_ADDRESS=temporal:7233
      - TEMPORAL_NAMESPACE=shipsec-prod
      - TEMPORAL_TASK_QUEUE=shipsec-prod
      - MINIO_ROOT_USER=minioadmin
      - MINIO_ROOT_PASSWORD=minioadmin
      - MINIO_ENDPOINT=minio
      - MINIO_PORT=9000
      - LOKI_URL=http://loki:3100
      - TERMINAL_REDIS_URL=redis://redis:6379
      - LOG_KAFKA_BROKERS=redpanda:9092
      - LOG_KAFKA_TOPIC=telemetry.logs
      - LOG_KAFKA_CLIENT_ID=shipsec-backend
      - LOG_KAFKA_GROUP_ID=shipsec-backend-log-consumer
      - EVENT_KAFKA_TOPIC=telemetry.events
      - EVENT_KAFKA_CLIENT_ID=shipsec-backend-events
      - EVENT_KAFKA_GROUP_ID=shipsec-event-ingestor
      - AUTH_PROVIDER=${AUTH_PROVIDER:-local}
      - CLERK_PUBLISHABLE_KEY=${CLERK_PUBLISHABLE_KEY:-}
      - CLERK_SECRET_KEY=${CLERK_SECRET_KEY:-}
      # Set to 'true' to disable analytics
      - DISABLE_ANALYTICS=${DISABLE_ANALYTICS:-false}
      # Secret encryption key (must be exactly 32 characters, NOT hex-encoded)
      - SECRET_STORE_MASTER_KEY=${SECRET_STORE_MASTER_KEY:-CHANGE_ME_32_CHAR_SECRET_KEY!!!!}
      # Internal service-to-service auth token (must match worker)
      - INTERNAL_SERVICE_TOKEN=${INTERNAL_SERVICE_TOKEN:-internal-service-token}
    ports:
      - '3211:3211'
    depends_on:
      postgres:
        condition: service_healthy
      temporal:
        condition: service_started
      minio:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    image: ghcr.io/shipsecai/studio-frontend:${SHIPSEC_TAG:-latest}
    build:
      context: ..
      dockerfile: Dockerfile
      target: frontend
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:3211}
        VITE_BACKEND_URL: ${VITE_BACKEND_URL:-http://localhost:3211}
        VITE_AUTH_PROVIDER: ${VITE_AUTH_PROVIDER:-clerk}
        VITE_DEFAULT_ORG_ID: ${VITE_DEFAULT_ORG_ID:-}
        VITE_CLERK_PUBLISHABLE_KEY: ${VITE_CLERK_PUBLISHABLE_KEY:-}
        VITE_GIT_SHA: ${GIT_SHA:-unknown}
        VITE_PUBLIC_POSTHOG_KEY: ${VITE_PUBLIC_POSTHOG_KEY:-}
        VITE_PUBLIC_POSTHOG_HOST: ${VITE_PUBLIC_POSTHOG_HOST:-}
    container_name: shipsec-frontend
    # NOTE: Auth defaults to Clerk intentionally - production requires Clerk authentication.
    # Set VITE_AUTH_PROVIDER=local in .env only for local development without Clerk.
    environment:
      - VITE_API_URL=${VITE_API_URL:-http://localhost:3211}
      - VITE_BACKEND_URL=${VITE_BACKEND_URL:-http://localhost:3211}
      - VITE_AUTH_PROVIDER=${VITE_AUTH_PROVIDER:-clerk}
      - VITE_DEFAULT_ORG_ID=${VITE_DEFAULT_ORG_ID:-}
      - VITE_CLERK_PUBLISHABLE_KEY=${VITE_CLERK_PUBLISHABLE_KEY:-}
    ports:
      - '8090:8080'
    depends_on:
      - backend
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:8080']
      interval: 30s
      timeout: 10s
      retries: 5

  worker:
    image: ghcr.io/shipsecai/studio-worker:${SHIPSEC_TAG:-latest}
    build:
      context: ..
      dockerfile: Dockerfile
      target: worker
    container_name: shipsec-worker
    environment:
      - NODE_ENV=production
      - SHIPSEC_ENV=production
      - DATABASE_URL=postgresql://shipsec:shipsec@postgres:5432/shipsec
      - ENABLE_INGEST_SERVICES=true
      - TEMPORAL_ADDRESS=temporal:7233
      - TEMPORAL_NAMESPACE=shipsec-prod
      - TEMPORAL_TASK_QUEUE=shipsec-prod
      - MINIO_ROOT_USER=minioadmin
      - MINIO_ROOT_PASSWORD=minioadmin
      - MINIO_ENDPOINT=minio
      - MINIO_PORT=9000
      - LOKI_URL=http://loki:3100
      - TERMINAL_REDIS_URL=redis://redis:6379
      - DOCKER_HOST=tcp://dind:2375
      - LOG_KAFKA_BROKERS=redpanda:9092
      - LOG_KAFKA_TOPIC=telemetry.logs
      - LOG_KAFKA_CLIENT_ID=shipsec-worker
      - EVENT_KAFKA_TOPIC=telemetry.events
      - EVENT_KAFKA_CLIENT_ID=shipsec-worker-events
      # Secret encryption key (must be exactly 32 characters, NOT hex-encoded)
      - SECRET_STORE_MASTER_KEY=${SECRET_STORE_MASTER_KEY:-CHANGE_ME_32_CHAR_SECRET_KEY!!!!}
      # Internal service-to-service auth token (must match backend)
      - INTERNAL_SERVICE_TOKEN=${INTERNAL_SERVICE_TOKEN:-internal-service-token}
      # Backend URL for internal API calls
      - STUDIO_API_BASE_URL=http://backend:3211/api/v1
    depends_on:
      postgres:
        condition: service_healthy
      temporal:
        condition: service_started
      minio:
        condition: service_healthy
      dind:
        condition: service_healthy
      redis:
        condition: service_healthy
      redpanda:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'node', '-e', 'process.exit(0)']
      interval: 30s
      timeout: 10s
      retries: 5

volumes:
  postgres_data:
  minio_data:
  loki_data:
  temporal_data:
  docker_data:
  redis_data:
  redpanda_data:

networks:
  default:
    name: shipsec-network
